# ===============================
# Nombre de la aplicación
# ===============================
spring.application.name=proyectoTFG
server.port=8081
app.base-url=${APP_BASE_URL:http://localhost:8081}
app.jwt.secret=${APP_JWT_SECRET:fidelyfood-dev-jwt-secret-change-me-2026}
# ===============================
# Configuración de MySQL (local vía Homebrew por defecto; DB_URL la
# sobreescribe en producción, p.ej. la URL JDBC que da Railway)
# ===============================
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/proyectoTFG?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:fidelyfood_app}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# CORS — orígenes adicionales permitidos en producción (frontend real),
# separados por comas. Los orígenes de desarrollo (localhost/Capacitor)
# siempre están permitidos, ver SecurityConfig.
# ===============================
app.cors.extra-origins=${APP_CORS_ORIGINS:}

# ===============================
# Subida de ficheros (fotos de perfil / restaurante)
# ===============================
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# ===============================
# Configuración JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===============================
# Logs (debug útil para desarrollo)
# ===============================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springdoc=DEBUG

# ===============================
# (Opcional) Base path API
# ===============================
# spring.data.rest.base-path=/api